import React from 'react';

import { Heading } from '../../components';

import * as Styled from './styles';

const SocialComments = () => (
  <Styled.Wrapper>
    <Styled.TextWrapper>
      <Heading text="O que estão falando nas redes sociais" />
    </Styled.TextWrapper>
  </Styled.Wrapper>
);

export default SocialComments;
