import { Canvas, Meta, Story } from '@storybook/addon-docs'
import Button from '.'
import * as stories from './stories'

<Meta
  title="Design System/Button"
  component={Button}
  args={{
    text: 'Clique aqui',
    size: 'large',
    color: 'blue',
    disabled: false,
    fullWidth: false
  }}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large'],
      control: { type: 'inline-radio' }
    },
    color: {
      options: ['gray', 'blue'],
      control: { type: 'inline-radio' }
    },
    onClick: { action: 'onClick' }
  }}
  parameters={{ controls: { exclude: ['icon'], sort: 'requiredFirst' } }}
/>

# Button

This is a general purpose button component.

### Default

The button receives props for styling, icons, as well as a click event,
and makes its reference available through the forwardRef() hook.

<Canvas>
  <Story story={stories.Default} />
</Canvas>
