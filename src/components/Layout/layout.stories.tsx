import React from 'react';
import { Meta } from '@storybook/react';
import Layout from '.';

export default {
  title: 'Base Layout',
  component: Layout,
} as Meta;

export const LayoutDefault = () => <Layout />;
