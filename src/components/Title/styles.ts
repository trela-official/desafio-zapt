import styled from 'styled-components'

export const Header = styled.header`
  max-width:1250px;
  margin: 0 auto;
  `

export const Title = styled.h1`
  font-weight:300;
  text-align: center;
  font-size: 2.5rem;
  `