import { Canvas, Meta, Story } from '@storybook/addon-docs'
import SocialComments from '.'
import * as stories from './stories'

<Meta
  title="Design System/SocialComments"
  component={SocialComments}
  parameters={{
    controls: { sort: 'requiredFirst' }
  }}
/>

# SocialComments

This component shows user comments made on social networks.

## Default

This component can be powered by data from an api.

The component shows the user's comment, name, image and link to the social network profile.

<Canvas>
  <Story story={stories.Default} />
</Canvas>
